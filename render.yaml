services:
  - type: web
    name: video-call-app-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      # Let React Router handle direct visits and browser refreshes on client routes.
      - type: rewrite
        source: /*
        destination: /index.html
